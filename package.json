{
  "name": "ai-detector-college-admissions",
  "version": "1.0.0",
  "description": "AI Detector for College Admissions Essays",
  "scripts": {
    "backend": "cd backend && source venv/bin/activate && python3 run.py",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  }
}
